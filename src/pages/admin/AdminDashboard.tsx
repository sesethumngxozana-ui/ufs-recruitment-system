import { Route,Routes } from 'react-router-dom'; import DashboardLayout from '../../components/layout/DashboardLayout'; import { Card,Section } from '../../components/ui/common'; import UserManagement from './sections/UserManagement'; import Analytics from './sections/Analytics'
const links=[{to:'/admin',label:'Overview',end:true},{to:'/admin/users',label:'User Management'},{to:'/admin/jobs',label:'Job Listings'},{to:'/admin/analytics',label:'Analytics'},{to:'/admin/settings',label:'System Settings'}];function Overview(){return <Section title="Platform overview"><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:'16px'}}>{[['1,248','Total Students'],['86','Total Employers'],['42','Active Jobs'],['3,691','Total Applications']].map(s=><Card key={s[1]}><b style={{fontSize:'2rem',color:'var(--color-ufs-gold)'}}>{s[0]}</b><p style={{color:'var(--color-ufs-muted)'}}>{s[1]}</p></Card>)}</div></Section>};export default function AdminDashboard(){return <Routes><Route element={<DashboardLayout role="Administrator" links={links}/>}><Route index element={<Overview/>}/><Route path="users" element={<UserManagement/>}/><Route path="analytics" element={<Analytics/>}/><Route path="*" element={<Overview/>}/></Route></Routes>}

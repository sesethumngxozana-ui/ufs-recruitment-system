import { Route,Routes } from 'react-router-dom'; import DashboardLayout from '../../components/layout/DashboardLayout'; import { Card,Section } from '../../components/ui/common'; import PostJob from './sections/PostJob'; import CandidateSearch from './sections/CandidateSearch'; import InterviewScheduler from './sections/InterviewScheduler'
const links=[{to:'/employer',label:'Dashboard',end:true},{to:'/employer/post-job',label:'Post a Job'},{to:'/employer/candidates',label:'Candidate Search'},{to:'/employer/applications',label:'Applications Received'},{to:'/employer/interviews',label:'Interview Scheduler'},{to:'/employer/company',label:'Company Profile'},{to:'/employer/settings',label:'Settings'}];function Overview(){return <Section title="Employer dashboard"><p style={{color:'var(--color-ufs-green)',marginTop:'-16px'}}>Your hiring activity at a glance.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(170px,1fr))',gap:'16px'}}>{[['6','Jobs Posted'],['42','Applications Received'],['8','Interviews Scheduled'],['11','Candidates Shortlisted']].map(x=><Card key={x[1]}><b style={{fontSize:'2rem',color:'var(--color-ufs-gold)'}}>{x[0]}</b><div style={{color:'var(--color-ufs-muted)',fontSize:'.86rem'}}>{x[1]}</div></Card>)}</div></Section>};export default function EmployerDashboard(){return <Routes><Route element={<DashboardLayout role="Employer" links={links}/>}><Route index element={<Overview/>}/><Route path="post-job" element={<PostJob/>}/><Route path="candidates" element={<CandidateSearch/>}/><Route path="interviews" element={<InterviewScheduler/>}/><Route path="*" element={<Overview/>}/></Route></Routes>}

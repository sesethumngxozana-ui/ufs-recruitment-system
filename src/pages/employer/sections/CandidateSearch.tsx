import { useState } from 'react'; import { mockStudents } from '../../../data/mockStudents'; import { Card,Section,Select,buttonStyle } from '../../../components/ui/common'
export default function CandidateSearch(){const [short,setShort]=useState<string[]>([]);return <Section title="Candidate Search"><Card><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(160px,1fr))',gap:'12px'}}><Select><option>All faculties</option><option>Natural & Agricultural Sciences</option><option>Health Sciences</option></Select><Select><option>All degree types</option><option>Bachelor's</option><option>Honours</option></Select><label style={{color:'var(--color-ufs-muted)',fontSize:'.85rem'}}>Minimum GPA: 3.0<input type="range" min="0" max="4" step=".1" defaultValue="3" style={{width:'100%',accentColor:'var(--color-ufs-green)'}}/></label></div><div style={{display:'flex',gap:'8px',marginTop:'14px',flexWrap:'wrap'}}>{['React','Python','SQL','Research','Writing'].map(s=><button key={s} style={{...buttonStyle,padding:'6px 10px',background:'var(--color-ufs-dark)',border:'1px solid var(--color-ufs-border)'}}>{s}</button>)}</div></Card><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:'16px'}}>{mockStudents.map(s=><Card key={s.id}><div style={{display:'flex',gap:'12px'}}><div style={{width:'45px',height:'45px',borderRadius:'50%',background:'var(--color-ufs-green)',display:'grid',placeItems:'center'}}>{s.name.split(' ').map(x=>x[0]).join('')}</div><div><h2 style={{fontSize:'1.05rem'}}>{s.name}</h2><p style={{color:'var(--color-ufs-muted)',fontSize:'.8rem'}}>{s.degree}</p></div></div><p style={{color:'var(--color-ufs-muted)',fontSize:'.83rem',margin:'12px 0'}}>{s.faculty} · GPA {s.gpa}</p><div style={{display:'flex',gap:'5px',flexWrap:'wrap'}}>{s.skills.slice(0,3).map(x=><span key={x} style={{fontSize:'.75rem',padding:'3px 7px',border:'1px solid var(--color-ufs-green)',borderRadius:'999px'}}>{x}</span>)}</div><div style={{display:'flex',gap:'8px',marginTop:'15px'}}><button style={{...buttonStyle,flex:1,background:'var(--color-ufs-dark)',border:'1px solid var(--color-ufs-border)'}}>View Profile</button><button onClick={()=>setShort(short.includes(s.id)?short.filter(x=>x!==s.id):[...short,s.id])} style={{...buttonStyle,flex:1}}>{short.includes(s.id)?'★ Shortlisted':'☆ Shortlist'}</button></div></Card>)}</div></Section>}

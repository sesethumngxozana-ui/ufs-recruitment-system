import { useMemo,useState } from 'react'; import { mockJobs } from '../../../data/mockJobs'; import { Card, Input, Section, Select, buttonStyle } from '../../../components/ui/common'
export default function JobBoard(){const [search,setSearch]=useState(''),[type,setType]=useState(''),[applied,setApplied]=useState<string[]>([]);const jobs=useMemo(()=>mockJobs.filter(j=>(!search||`${j.title} ${j.companyName}`.toLowerCase().includes(search.toLowerCase()))&&(!type||j.type===type)),[search,type]);return <Section title="Job Board"><div style={{display:'grid',gridTemplateColumns:'2fr 1fr 1fr',gap:'10px'}}><Input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search jobs or companies"/><Select value={type} onChange={e=>setType(e.target.value)}><option value="">All job types</option>{['full-time','part-time','internship','contract'].map(x=><option key={x}>{x}</option>)}</Select><Select><option>All faculties</option><option>Natural Sciences</option><option>Economic Sciences</option></Select></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:'16px'}}>{jobs.map(j=><Card key={j.id} style={{display:'grid',gap:'12px'}}><div><p style={{color:'var(--color-ufs-gold)',fontSize:'.85rem'}}>{j.companyName}</p><h2 style={{fontSize:'1.13rem'}}>{j.title}</h2></div><p style={{color:'var(--color-ufs-muted)',fontSize:'.88rem'}}>{j.location} · <span style={{textTransform:'capitalize'}}>{j.type}</span></p><p style={{color:'var(--color-ufs-muted)',fontSize:'.85rem'}}>{j.description}</p><small style={{color:'var(--color-ufs-muted)'}}>Posted {j.postedAt}</small><button onClick={()=>setApplied([...applied,j.id])} disabled={applied.includes(j.id)} style={{...buttonStyle,background:applied.includes(j.id)?'var(--color-ufs-green)':'var(--color-ufs-green)'}}>{applied.includes(j.id)?'Applied ✓':'Apply Now'}</button></Card>)}</div></Section>}
